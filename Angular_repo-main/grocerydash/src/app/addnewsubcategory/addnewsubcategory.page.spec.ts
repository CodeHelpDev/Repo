import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AddnewsubcategoryPage } from './addnewsubcategory.page';

describe('AddnewsubcategoryPage', () => {
  let component: AddnewsubcategoryPage;
  let fixture: ComponentFixture<AddnewsubcategoryPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AddnewsubcategoryPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
